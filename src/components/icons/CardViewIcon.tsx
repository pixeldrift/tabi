import Icon from "./svg/card-view.svg?react";

/** Card view — two stacked wide tiles, each with its own corner radius, for
 *  the two-column card layout. Source: ./svg/card-view.svg (edit there —
 *  this file just re-exports it). */
export const CardViewIcon = Icon;
