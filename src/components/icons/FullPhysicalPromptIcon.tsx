import Icon from "./svg/full-physical-prompt.svg?react";

/** Full physical prompt — a mirrored pair of open, cupped hands (each an
 *  outer hand/wrist edge plus a curled-finger stroke) whose inner edges
 *  meet and merge into a single shared stem at the bottom (full
 *  hand-over-hand support) — the "two hands" counterpart to
 *  PartialPhysicalPromptIcon's single one. Source:
 *  ./svg/full-physical-prompt.svg (edit there — this file just re-exports
 *  it). */
export const FullPhysicalPromptIcon = Icon;
