import Icon from "./svg/verbal-prompt.svg?react";

/** Verbal prompt — a rounded, empty speech bubble (no text lines, so it
 *  reads as spoken rather than texted) with its tail hooking off the right
 *  edge and curling back to the left. Source: ./svg/verbal-prompt.svg (edit
 *  there — this file just re-exports it as a component). */
export const VerbalPromptIcon = Icon;
