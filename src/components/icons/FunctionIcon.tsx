import Icon from "./svg/function.svg?react";

/** Function — a thought bubble (a cloud + trailing dots, no speech-bubble
 *  tail), for the behavioral function (attention/escape/tangible/sensory)
 *  half of the ABC framework alongside AntecedentIcon. Not yet wired to a
 *  specific phase; added to the icon set for future use. Source:
 *  ./svg/function.svg (edit there — this file just re-exports it). */
export const FunctionIcon = Icon;
