import Icon from "./svg/daily.svg?react";

/** Daily (rotation) schedule — a sun with a clock hand and short hour-mark
 *  rays in place of a full sunburst, reading as "today" + "time" together.
 *  Used for the Schedule tab while it's still just a single recurring
 *  weekday grid — CalendarDays stays reserved for a future real calendar
 *  view. Source: ./svg/daily.svg (edit there — this file just re-exports
 *  it). */
export const DailyIcon = Icon;
