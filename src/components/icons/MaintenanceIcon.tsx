import Icon from "./svg/maintenance.svg?react";

/** Maintenance — a watering can, keeping an already-learned skill going.
 *  Source: ./svg/maintenance.svg (edit there — this file just re-exports
 *  it). */
export const MaintenanceIcon = Icon;
