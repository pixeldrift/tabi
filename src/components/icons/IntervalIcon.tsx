import Icon from "./svg/interval.svg?react";

/** Interval — a plain analog clock face (round hour marks, hour + minute
 *  hand), deliberately without the stopwatch's top crown/knob so it reads
 *  as "a point in time" rather than "an elapsed duration" at a glance.
 *  Source: ./svg/interval.svg (edit there — this file just re-exports it). */
export const IntervalIcon = Icon;
