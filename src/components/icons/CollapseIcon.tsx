import Icon from "./svg/collapse.svg?react";

/** Two chevrons folding toward each other — "collapse this", not "hide
 *  this". Source: ./svg/collapse.svg (edit there — this file just
 *  re-exports it). */
export const CollapseIcon = Icon;
