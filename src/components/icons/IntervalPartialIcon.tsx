import Icon from "./svg/interval-partial.svg?react";

/** Interval, Partial Interval Recording variant — same clock face and
 *  sideways-curly-brace bracket as IntervalWholeIcon (same apex depth, so
 *  it reads as the same shape narrowed rather than a different one), but
 *  only spanning half the width: any portion of the interval counts, not
 *  the whole span.
 *  Source: ./svg/interval-partial.svg. */
export const IntervalPartialIcon = Icon;
