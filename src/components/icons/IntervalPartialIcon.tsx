import Icon from "./svg/interval-partial.svg?react";

/** Interval, Partial Interval Recording variant — same clock face as
 *  IntervalWholeIcon, but the supporting bracket only spans half the
 *  width: any portion of the interval counts, not the whole span.
 *  Source: ./svg/interval-partial.svg. */
export const IntervalPartialIcon = Icon;
