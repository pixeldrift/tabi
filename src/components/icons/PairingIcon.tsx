import Icon from "./svg/pairing.svg?react";

/** Pairing — two overlapping people, building rapport before instruction.
 *  Distinct from the handshake co-treat icon (that's two providers working
 *  together; this is a provider building rapport with the learner). Not yet
 *  wired to a specific phase; added to the icon set for future use. Source:
 *  ./svg/pairing.svg (edit there — this file just re-exports it). */
export const PairingIcon = Icon;
