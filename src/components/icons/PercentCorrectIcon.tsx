import Icon from "./svg/percent-correct.svg?react";

/** Percent Correct — percent glyph with a checkmark and a tiny x in place of
 *  the two circles. Source: ./svg/percent-correct.svg (edit there — this
 *  file just re-exports it as a component). */
export const PercentCorrectIcon = Icon;
