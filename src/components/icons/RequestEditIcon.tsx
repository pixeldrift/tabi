import Icon from "./svg/request-edit.svg?react";

/** Lucide's own Pencil glyph with a small "?" tucked into the empty
 *  diagonal space below its tip, subscript-style — marks an edit as a
 *  REQUEST (goes to a supervisor for approval) rather than a direct edit,
 *  as one glyph rather than an icon plus a separately-positioned overlay.
 *  Source: ./svg/request-edit.svg. */
export const RequestEditIcon = Icon;
