import Icon from "./svg/timestamp.svg?react";

/** Timestamp — a plain analog clock face (round hour marks, hour + minute
 *  hand), deliberately without the stopwatch's top crown/knob so it reads
 *  as "a point in time" rather than "an elapsed duration" at a glance.
 *  Source: ./svg/timestamp.svg (edit there — this file just re-exports it). */
export const TimestampIcon = Icon;
