import Icon from "./svg/frequency.svg?react";

/** Frequency — an italicized (slanted) number sign. Source:
 *  ./svg/frequency.svg (edit there — this file just re-exports it). */
export const FrequencyIcon = Icon;
