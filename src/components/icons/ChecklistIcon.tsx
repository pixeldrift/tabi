import Icon from "./svg/checklist.svg?react";

/** Checklist — a short list of checked/unchecked boxes. Source:
 *  ./svg/checklist.svg (edit there — this file just re-exports it). */
export const ChecklistIcon = Icon;
