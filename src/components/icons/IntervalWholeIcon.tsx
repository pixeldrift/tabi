import Icon from "./svg/interval-whole.svg?react";

/** Interval, Whole Interval Recording variant — the plain clock face,
 *  shifted up to leave room for a bracket underneath spanning its full
 *  width: the whole span "supports" the clock, matching the sampling rule
 *  (score only if the behavior occurs for the entire interval).
 *  Source: ./svg/interval-whole.svg. */
export const IntervalWholeIcon = Icon;
