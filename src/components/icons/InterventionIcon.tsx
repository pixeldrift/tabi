import Icon from "./svg/intervention.svg?react";

/** Intervention — a tilted rectangle and an upright one, split by a dashed
 *  line: a condition changing partway through. Source:
 *  ./svg/intervention.svg (edit there — this file just re-exports it). */
export const InterventionIcon = Icon;
