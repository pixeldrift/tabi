import Icon from "./svg/rate.svg?react";

/** Rate ("how many times per") — a clock face whose last quarter (9 o'clock
 *  to 12 o'clock) is drawn as dots instead of a solid arc, distinguishing it
 *  from Duration's plain stopwatch at a glance. Source: ./svg/rate.svg (edit
 *  there — this file just re-exports it). */
export const RateIcon = Icon;
