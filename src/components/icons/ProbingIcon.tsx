import Icon from "./svg/probing.svg?react";

/** Probing — a thermometer, checking the current level before deciding what
 *  to do next. Source: ./svg/probing.svg (edit there — this file just
 *  re-exports it). */
export const ProbingIcon = Icon;
