import Icon from "./svg/list-view.svg?react";

/** List view — three full-width rows, each a title line over a shorter
 *  detail line, echoing the stacked single-column card list. Source:
 *  ./svg/list-view.svg (edit there — this file just re-exports it). */
export const ListViewIcon = Icon;
