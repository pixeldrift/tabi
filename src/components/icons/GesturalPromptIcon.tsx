import Icon from "./svg/gestural-prompt.svg?react";

/** Gestural prompt — a pointing cursor/arrow. Source:
 *  ./svg/gestural-prompt.svg (edit there — this file just re-exports it). */
export const GesturalPromptIcon = Icon;
