import Icon from "./svg/baseline.svg?react";

/** Baseline — an axis with a flat (dashed) run before data starts trending
 *  up, the pre-intervention level being measured. Source: ./svg/baseline.svg
 *  (edit there — this file just re-exports it). */
export const BaselineIcon = Icon;
