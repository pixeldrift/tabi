import Icon from "./svg/grid-view.svg?react";

/** Large grid — a 2x2 array of quick-action tiles, two per row. Source:
 *  ./svg/grid-view.svg (edit there — this file just re-exports it). */
export const GridViewIcon = Icon;
