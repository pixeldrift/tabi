import Icon from "./svg/fading.svg?react";

/** Fading — a ghost, for fading prompts/support out over time. Not yet wired
 *  to a specific phase; added to the icon set for future use. Source:
 *  ./svg/fading.svg (edit there — this file just re-exports it). */
export const FadingIcon = Icon;
