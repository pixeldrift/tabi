import Icon from "./svg/interval-momentary.svg?react";

/** Interval, Momentary Time Sampling variant — same clock face, but no
 *  spanning bracket at all: a single dot on a short connector, reading as
 *  "just this one instant," not any stretch of time.
 *  Source: ./svg/interval-momentary.svg. */
export const IntervalMomentaryIcon = Icon;
