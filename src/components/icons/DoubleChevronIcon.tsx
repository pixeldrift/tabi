import Icon from "./svg/double-chevron.svg?react";

/** Two solid, rounded-tip chevrons pointing away from each other, no
 *  connecting bar between them — signals "drag either direction," used on
 *  the details drawer's pull tab once it's open (dragging left keeps
 *  expanding toward full width, dragging right collapses/closes), where a
 *  single directional chevron would only describe one of the two.
 *  Source: ./svg/double-chevron.svg (edit there — this file just
 *  re-exports it). */
export const DoubleChevronIcon = Icon;
