import Icon from "./svg/modeling-prompt.svg?react";

/** Modeling prompt — a framed mirror with two reflection-glint lines
 *  (demonstrate the behavior for the learner to reflect/imitate). Source:
 *  ./svg/modeling-prompt.svg (edit there — this file just re-exports
 *  it). */
export const ModelingPromptIcon = Icon;
