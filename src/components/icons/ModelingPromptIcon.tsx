import Icon from "./svg/modeling-prompt.svg?react";

/** Modeling prompt — two overlapping squares (demonstrate, then copy).
 *  Source: ./svg/modeling-prompt.svg (edit there — this file just
 *  re-exports it). */
export const ModelingPromptIcon = Icon;
